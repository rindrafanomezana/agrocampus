pages:
  image: pnavaro/jupyter
  script:
    - julia -e `import Pkg; Pkg.activate(@__DIR__); Pkg.instantiate()`
    - pip install nbcourse
    - nbcourse -n 1
  tags:
    - docker
    - pages
  only:
    - master
  artifacts:
    paths:
      - build
