pages:
  image: pnavaro/jupyter
  script:
    - pip install nbcourse
    - nbcourse -n 1
    - mv build public
  tags:
    - docker
    - pages
  only:
    - master
  artifacts:
    paths:
      - public
